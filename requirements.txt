# Core
websockets>=14             # Deepgram streaming (raw WS client).
                           # 14.0 is where the top-level `websockets.connect`
                           # became the new asyncio client, which is the one
                           # taking `additional_headers`. On 12/13 stt.py
                           # raises TypeError at connect.
python-dotenv>=1.0         # .env loading

# Both lanes. The fast lane runs through this too, so there is no `anthropic`
# dependency here — nothing in src/ calls the Messages API.
# Also requires the Claude Code CLI on PATH:  npm i -g @anthropic-ai/claude-code
claude-agent-sdk>=0.1

# Local STT — the default backend (STT_BACKEND=local). No key, nothing leaves
# the machine. CPU works; a CUDA GPU is much faster.
faster-whisper>=1.0
numpy
